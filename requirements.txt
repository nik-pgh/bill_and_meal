torch>=2.0
torchvision
# Gemma 4 (model_type "gemma4") isn't in stable transformers releases yet — pull
# from main. Pin to a specific commit if main breaks.
transformers @ git+https://github.com/huggingface/transformers.git
accelerate
# PEFT also pulled from main: stable PEFT doesn't recognize the
# Gemma4ClippableLinear wrapper around Linear4bit, so LoRA attach raises
# ValueError. Pin to a commit if main breaks.
peft @ git+https://github.com/huggingface/peft.git
trl
bitsandbytes
datasets
Pillow
wandb
anthropic
python-dotenv
pyyaml
evaluate
bert-score
pytest
